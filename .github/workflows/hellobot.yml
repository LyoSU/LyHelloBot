name: HelloBot
on:
  schedule:
    - cron: '*/1 * * * *'
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      uses: actions/setup-node@v1
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
